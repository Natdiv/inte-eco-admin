name: inte_eco_admin
description: Admin inte eco app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  firebase_core: ^2.3.0
  get: ^4.6.5
  cloud_firestore: ^4.1.0
  google_fonts: ^2.1.0
  flutter_svg: ^0.22.0
  data_table_2: ^2.0.3
  fl_chart: ^0.55.2
  firebase_auth: ^4.2.1
  cloud_functions: ^4.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/images/
    - assets/icons/
    - assets/logo/
  uses-material-design: true

